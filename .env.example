# Copy to .env and edit.
# WORKO_ADMIN_TOKEN is required; use a hard-to-guess random string on any public-facing server.
WORKO_ADMIN_TOKEN=change-me-admin-secret

# Member join_tokens are auto-generated by the dashboard when creating a workspace — not configured here.
